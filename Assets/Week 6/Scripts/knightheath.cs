using UnityEngine.UI;
using UnityEngine.EventSystems;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class knightheath : MonoBehaviour
{  
    public Text Health;
    public Text PreHealth; 


    // Start is called before the first frame update
    void Start()
    {
       // PreHealth.text = PlayerPrefs.GetInt(PreHealth, 0). ToString();
    }

    // Update is called once per frame
    void Update()
    {
      //  PlayerPrefs.SetInt(PreHealth. number);
    }
}
